"use client";
const TokenBtn = () => {
  return (
    <button
      onClick={() => {
        alert("Token Submitted");
      }}
    >
      Click To Alert
    </button>
  );
};

export default TokenBtn;
