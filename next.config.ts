import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  //basePath: "/testt",
  output: "standalone",
  // trailingSlash: true,
  // assetPrefix: "/testt/",
};

export default nextConfig;
